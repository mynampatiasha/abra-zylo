import 'package:flutter/material.dart';

class ProductReturn extends StatelessWidget {
  const ProductReturn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
