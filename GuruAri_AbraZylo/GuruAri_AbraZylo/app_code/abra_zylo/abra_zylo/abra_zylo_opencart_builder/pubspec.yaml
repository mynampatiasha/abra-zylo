name: oc_demo
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 2.8.6+87

environment:
  sdk: ">=3.2.3 <4.0.0"
  # sdk: ">=3.1.0 <3.10.1"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  retrofit:
  dio: ^5.4.0
  shared_preferences: ^2.5.5
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  dio_cookie_manager: ^3.2.0
  path_provider: ^2.1.5
  simple_animations: 4.0.2
  carousel_slider: ^5.1.2
  # Keep 6.2.2 — there is no 6.2.3, next version is 7.x which breaks Firebase pods.
  # google_sign_in commented out — causes dart:js_interop build failure on Android
  # via google_sign_in_web pulling in web-only dart:js_interop into the Android build.
  # Re-enable once Flutter SDK is upgraded to a version that handles this correctly.
  # google_sign_in: 6.2.2
  firebase_core: ^3.6.0
#  flutter_login_facebook: ^1.5.0+1
  webview_flutter: ^4.13.1
  permission_handler: ^12.0.1
  flutter_html:
  file_picker: ^11.0.2
  http: ^1.1.0
  share_plus: ^11.1.0
  lottie: ^3.0.0
  local_auth: ^2.1.7
  location: ^5.0.3
  geocoding: ^4.0.0
  google_maps_flutter: ^2.14.0
  mobile_scanner: ^3.5.5
  speech_to_text: ^7.3.0
  firebase_messaging: ^15.1.3
  flutter_rating_bar: ^4.0.1
  flutter_local_notifications: ^19.4.2
  url_launcher: ^6.3.2
  flutter_quill: ^8.5.0
  http_parser:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cached_network_image: ^3.4.1
  visibility_detector: ^0.4.0+2
  google_fonts: ^6.2.1
  get_storage:
  fl_chart: any
  image_picker: ^1.2.2
  date_format: ^2.0.7
  flutter_polyline_points:
  firebase_database: ^11.1.4
  flutter_switch: ^0.3.2
  cookie_jar: ^4.0.8
  razorpay_flutter: ^1.4.0
  photo_view: ^0.15.0

dependency_overrides:
  # Dart 3.10 removed old unmodifiable typed-list view aliases used by older transitive releases.
  archive: ^4.0.9
  # Flutter 3.38 removed v1 Android embedding Registrar APIs used by older plugin transitive locks.
  fluttertoast: ^8.2.4
  flutter_plugin_android_lifecycle: ^2.0.34
  sqflite: ^2.4.2
  win32: ^5.15.0
  # AGP 8.x requires namespace in build.gradle; 5.4.1 is missing it — force newer version.
  flutter_keyboard_visibility: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.1
  retrofit_generator:
  build_runner:
  json_serializable:
  hive_generator: ^2.0.1
  notification_center: ^1.0.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
    - assets/languages/
    - assets/home.json
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
